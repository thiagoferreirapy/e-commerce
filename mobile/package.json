{
  "name": "torque-mobile",
  "version": "1.0.0",
  "private": true,
  "description": "App nativo (Capacitor) da loja TORQUE — shell sobre o site Next (SSR).",
  "scripts": {
    "sync": "cap sync",
    "android": "cap sync android && cap open android",
    "ios": "cap sync ios && cap open ios"
  },
  "dependencies": {
    "@capacitor/android": "^8.4.1",
    "@capacitor/core": "^8.4.1",
    "@capacitor/ios": "^8.4.1"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.4.1"
  }
}
